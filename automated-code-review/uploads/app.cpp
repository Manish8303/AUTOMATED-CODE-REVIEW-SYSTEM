#include <iostream>

// The main function where program execution begins
int main() {
    // std::cout is used to output data to the standard output device (screen)
    std::cout << "Hello, World!" << std::endl; 
    
    // Returning 0 indicates successful execution
    return 0;
}